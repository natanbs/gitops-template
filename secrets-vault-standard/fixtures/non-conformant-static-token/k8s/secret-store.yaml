apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: vault-store
spec:
  provider:
    vault:
      server: https://vault.example.com
      path: gitops-01
      auth:
        kubernetes:
          mountPath: /v1/auth/kubernetes
          role: app-role
