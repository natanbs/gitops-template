# app-profile fixture with no applicative inputs.
#
# No Dockerfile, no .env, no k8s/ manifests: exercising the N/A path —
# the structure check reports N/A (never FAIL), the run ends AUDIT RESULT: PASS.